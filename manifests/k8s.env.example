# Environment file for k8s service
# Copy to /etc/systemd/system/k8s.service.env and customize

# Server mode: single (SQLite) or ha (etcd)
SERVER_MODE=single

# Data directory
DATA_DIR=/var/lib/k8s

# Configuration file
CONFIG_FILE=/etc/k8s/config.yaml

# Additional options can be added here
